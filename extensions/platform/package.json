{
  "name": "pi-platform",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit -p .",
    "test": "node --test --test-timeout=30000 --experimental-strip-types *.test.ts"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "0.84.3",
    "@types/node": "^26.1.1",
    "typescript": "^7.0.2"
  }
}
